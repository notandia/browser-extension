{
  "name": "notandia-browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-browser extension for publisher context and explainable research-integrity signals.",
  "scripts": {
    "build": "node scripts/build-all.js",
    "build:target": "node scripts/build-target.js",
    "verify": "node scripts/verify-extension.js",
    "test": "node scripts/verify-extension.js && node --test tests/*.test.js"
  },
  "license": "AGPL-3.0-or-later",
  "devDependencies": {
    "linkedom": "^0.18.13"
  }
}
